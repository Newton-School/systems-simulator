import { Position } from 'reactflow'

/** Connection handle offsets - shared by all non-VPC node types via BaseNode. */
export const NODE_OFFSETS = ['25%', '50%', '75%']

/** Connection handle positions - shared by all non-VPC node types via BaseNode. */
export const NODE_POSITIONS = [Position.Left, Position.Top, Position.Right, Position.Bottom]
