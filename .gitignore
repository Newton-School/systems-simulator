node_modules
.pnpm-store
dist
out
.DS_Store
.eslintcache
*.log*
.claude
outputs
# Compute-contention migration harness: baseline.json is the committed reference;
# candidate.json is regenerated per-run (ephemeral).
.migration-snapshots/candidate.json
